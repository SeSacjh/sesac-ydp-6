const http = require('http');
const fs = require('fs');
const url = require('url');

http.createServer(function(req,res) {
    var q = parse(req, )
})

listen(8000, function() {
    console.log('8000포트 서버 연결 중');
})